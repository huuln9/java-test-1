enum TravelMode { walking, bike, motorcycle, car, water }

enum AlgorithmType { astar_route, alternative_route }

enum WeightingType { fastest }

enum ContextMenuType { single_point, polygon }

enum ResultPlaceFrom { vnpt_maps, google_maps }
