import 'app_config.dart';

class RouteConfig {
  static const String listRoute = "/${AppConfig.packageName}";
  static const String searchRoute = "/${AppConfig.packageName}/search";
  static const String detailRoute = "/${AppConfig.packageName}/detail";
}