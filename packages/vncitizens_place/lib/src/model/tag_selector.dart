import 'package:vncitizens_place/src/model/tag_content.dart';

class TagSelector {
  TagSelector({
    required this.data,
    required this.isSelected,
  });

  final TagContent data;
  bool isSelected;
}
